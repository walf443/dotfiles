[alias]
  ci = commit -v
  up = !sh -c 'git remote update && git config --get-regexp svn-remote > /dev/null && git svn fetch'
  co = checkout
  br = branch
  st = status
  di = diff
  df = diff --no-prefix
  wt = whatchanged
  svf = svn fetch
  svr = svn rebase
  svd = svn dcommit
  new = whatchanged -p HEAD@{1}..HEAD --reverse

